spring.application.name=leave-service
server.port=${PORT:8081}

# Cloud Database Connection (Aiven MySQL)
spring.datasource.url=jdbc:mysql://leave-db-college-portal.a.aivencloud.com:20867/defaultdb?ssl-mode=REQUIRED
spring.datasource.username=avnadmin
# Replace this with the password from your Aiven screen
spring.datasource.password=AVNS__bwCibJ16ttWHDuzlaT
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update

# Eureka Registration
eureka.client.serviceUrl.defaultZone=${EUREKA_SERVER_URL:http://localhost:8761/eureka/}
eureka.instance.hostname=${RENDER_EXTERNAL_HOSTNAME:localhost}

# The Shared Secret (Injected via Environment Variable during runtime)
college.app.jwtSecret=${JWT_SECRET}
# Expose Health Endpoint to keep Render awake
management.endpoints.web.exposure.include=health